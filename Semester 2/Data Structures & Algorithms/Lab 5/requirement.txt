ADT Matrix–represented as a sparse matrix where <line, column, value> triples (value ≠0) are memorized.  
The  elements  are  stored  in  a BST  with  linked  representation  with  dynamic allocation.